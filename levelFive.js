// Functions
function reverseArray(){
    // This function should accept an array as input
    // It should return the backwards version of the array
    // DO NOT use .reverse(), you can do this with a new array and a loop!
}

function greatestOfAllTime(){
    // This function should accept an array of objects
    // Each object will have two properties: "wins" and "name"
    // Return the "name" property of the object with the highest number of "wins"
}

module.exports = {
    reverseArray,
    greatestOfAllTime
}